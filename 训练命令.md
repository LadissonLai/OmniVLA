ssh大数据集上传命令
rsync -avP \
  --partial-dir=.rsync-partial \
  --timeout=300 \
  --info=progress2 \
  -e "ssh -o ServerAliveInterval=60 -o ServerAliveCountMax=3" \
  local_path \
  A100:server_path

经典vla训练方法付
export WANDB_MODE=offline 
torchrun --standalone --nnodes 1 --nproc-per-node 1 hf_vla/train_classicvla.py --vla_path ./omnivla-original

后台训练的方法：
tmux

输入命令
torchrun --standalone --nnodes 1 --nproc-per-node 1 hf_vla/train_classicvla.py --vla_path ./openvla-7b 2>&1 | tee train_classicvla_16_epoch.txt

训练和测试langpark
export WANDB_MODE=offline
torchrun --standalone --nnodes 1 --nproc-per-node 4 hf_vla/train_langpark.py 2>&1 | tee train_langpark_4H20_6epoch.txt
python3 hf_vla/test_langpark.py 或者
torchrun --standalone --nnodes 1 --nproc-per-node 4 hf_vla/test_langpark.py

消融实验
export WANDB_MODE=offline
torchrun --standalone --nnodes 1 --nproc-per-node 7 hf_vla/train_langpark_ablation_no_iam.py  2>&1 | tee train_langpark_ablation_no_iam_H20_6epoch.txt
测试
torchrun --standalone --nnodes 1 --nproc-per-node 7 hf_vla/test_langpark_ablation_no_iam.py


训练消融实验的命令：
torchrun --standalone --nnodes 1 --nproc-per-node 5 hf_vla/train_classicvla.py 2>&1 | tee train_classicvla_H20_6epoch.txt
torchrun --standalone --nnodes 1 --nproc-per-node 5 hf_vla/train_langpark_ablation_mem_only.py 2>&1 | tee train_langpark_ablation_mem_only.txt
torchrun --standalone --nnodes 1 --nproc-per-node 5 hf_vla/train_langpark_ablation_align_only.py 2>&1 | tee train_langpark_ablation_align_only.txt
消融实验的测试
torchrun --standalone --nnodes 1 --nproc-per-node 6 hf_vla/test_classicvla.py
torchrun --standalone --nnodes 1 --nproc-per-node 6 hf_vla/test_langpark_ablation_mem_only.py
torchrun --standalone --nnodes 1 --nproc-per-node 6 hf_vla/test_langpark_ablation_align_only.py


补充langpark的训练，补充测试集进行训练
export WANDB_MODE=offline
torchrun --standalone --nnodes 1 --nproc-per-node 2 hf_vla/train_langpark.py 2>&1 | tee train_langpark_H20_add2epoch.txt
torchrun --standalone --nnodes 1 --nproc-per-node 6 hf_vla/test_langpark.py